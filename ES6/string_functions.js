/* Use export to Share a Code Block */
const add = (x, y) => {
  return x + y;
};
export { add };

const uppercaseString = (string) => {
  return string.toUpperCase();
};
const lowercaseString = (string) => {
  return string.toLowerCase();
};
export { uppercaseString, lowercaseString, x };

let x = "gear";
